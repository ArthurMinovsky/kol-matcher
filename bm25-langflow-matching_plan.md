# Superseded BM25/Langflow Draft

The current approved implementation plan is
`kol-matcher-productionization_plan.md`. The current execution and verification
records are `doc/instruction/PLAN.md` and `doc/instruction/TEST.md`.

This file is retained only as a pointer so the older draft cannot be mistaken
for the active architecture.
